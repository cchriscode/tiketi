# ========================================
# TIKETI Kubernetes Secrets (Dev Environment)
# ========================================
#
# 이 파일은 예시 템플릿입니다.
# 실제 운영 시 secrets.env 파일을 생성하여 실제 값으로 교체하세요.
#
# ⚠️ secrets.env는 .gitignore에 포함되어 있어 커밋되지 않습니다.
# ========================================

# PostgreSQL Password
POSTGRES_PASSWORD=your-secure-postgres-password-here
DB_PASSWORD=your-secure-postgres-password-here

# JWT Secret (최소 32자 랜덤 문자열 권장)
JWT_SECRET=your-jwt-secret-at-least-32-characters-long

# Admin Account
ADMIN_EMAIL=admin@your-domain.com
ADMIN_PASSWORD=your-secure-admin-password

# AWS Credentials (로컬 개발 환경에서는 dummy 값 사용)
AWS_ACCESS_KEY_ID=dummy-aws-access-key
AWS_SECRET_ACCESS_KEY=dummy-aws-secret-key

# Toss Payments (https://docs.tosspayments.com/에서 발급)
# 개발: test_ck_, test_sk_ 로 시작하는 테스트 키 사용
# 운영: live_ck_, live_sk_ 로 시작하는 실제 키 사용
TOSS_CLIENT_KEY=test_ck_your_toss_client_key_here
TOSS_SECRET_KEY=test_sk_your_toss_secret_key_here

# Google OAuth Client Secret
# https://console.cloud.google.com/apis/credentials에서 발급
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Internal API Token (마이크로서비스 간 통신용)
# 랜덤 문자열 생성 권장: openssl rand -hex 32
INTERNAL_API_TOKEN=your-internal-api-token-here

# ========================================
# 설정 방법
# ========================================
# 1. 이 파일을 복사하여 secrets.env 생성:
#    cp secrets.env.example secrets.env
#
# 2. secrets.env 파일의 모든 값을 실제 값으로 교체
#
# 3. Kubernetes Secret 생성:
#    kubectl create secret generic tiketi-secret \
#      --from-env-file=k8s/overlays/dev/secrets.env \
#      -n tiketi
# ========================================
