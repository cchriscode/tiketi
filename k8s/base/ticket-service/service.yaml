apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  labels:
    app: ticket-service
spec:
  type: ClusterIP  # Internal service (NodePort in dev overlay)
  selector:
    app: ticket-service
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
      name: http
